export type SubjectType = {
  background: string;
  gradeRange: string;
  title: string;
  lessons: number;
  image: string;
};

export type BlogType = {
  date: number;
  month: string;
  title: string;
  author: string;
};