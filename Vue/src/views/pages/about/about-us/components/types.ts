export type AboutType = {
  title: string;
  progress: number;
};

export type AwardType = {
  name: string;
  year: number;
};

export type TeamMemberType = {
  img: string;
  name: string;
  designation: string;
};